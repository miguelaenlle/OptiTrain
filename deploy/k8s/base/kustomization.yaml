apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Shared shape. Everything that differs between a laptop and a GPU node lives
# in an overlay, so the difference is declared rather than discovered.
resources:
  - router.yaml
  - worker.yaml
commonLabels:
  app.kubernetes.io/part-of: optitrain-inference
