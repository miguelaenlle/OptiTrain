server {
  listen 80;
  root /usr/share/nginx/html;
  # No caching: pass 2 rewrites these files while Grafana polls, and a cached
  # CSV would make a live dashboard silently stale.
  add_header Cache-Control "no-store";
  add_header Access-Control-Allow-Origin "*";
  autoindex on;
}
